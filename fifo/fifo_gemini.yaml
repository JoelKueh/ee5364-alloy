
---
commands:
  - "run fill_and_then_empty for {d1,1} steps, {d1,2} Int\nfun capacity: Int { {1}}"
sweeps:
  - 1
deps:
  - - "2 * {v}"
    - "math.floor(math.log({v}) + 3.5)"
starting_point:
  - 1
names:
  - "RegsAndRobEntries"
  - "Steps"
timeout: "3600s"
